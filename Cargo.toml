[workspace]
members = [
	"kamaji"
]

[package]
name = "sparkle-kernel"
version = "0.1.0"
authors = ["Erin Moon <erin@hashbang.sh>"]

[dependencies]
x86_64 = "0.14"

[features]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
